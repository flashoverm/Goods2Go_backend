# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "netgloo_blog"
spring.datasource.url = jdbc:mysql://localhost:3306/goods2go

# Username and password
spring.datasource.username = root
spring.datasource.password = cocacola

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

#Temporary disable security to enable it remove this (Property not working
#perfectly. For disable security you need to Comment "@EnableWebSecurity" 
#in class WebSecurityConfig as well <Lucas-09/12/2017>)
#security.ignored=/**


# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.database=default

# PostgreSQL DB - "geo"
geo.datasource.url=jdbc:postgresql://localhost:5432/g2ggeo?currentSchema=public
geo.datasource.username=postgres
geo.datasource.password=cocacola
geo.datasource.driver-class-name=org.postgresql.Driver
geo.jpa.hibernate.ddl-auto = update

#Mail Config
mail.config.server.address=http://la.thral.de:8080
mail.config.webserver.address=http://web.goods2go.de

mail.config.verification.path=/verifyMail/
mail.config.verification.url=/user/verifyMail/
mail.config.verification.request.path=/requestVerification/
mail.config.verification.request.url=/user/requestVerification/
mail.config.sender.address=goods2go@thral.de


